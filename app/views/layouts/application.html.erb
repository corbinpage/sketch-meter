<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Sketch Meter" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Sketch Meter" %>">
    <%= stylesheet_link_tag "application", media: "all"%>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body role="document">
    <%= render 'layouts/navigation' %>
    <main role="main" class="container">
       <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
    <div id="footer">
      <div class="container">
        <a class="text-muted text-center" href="https://github.com/corbinpage/sketch-meter" target="_blank">About</a>
        <div class="text-muted">|</div>
        <a class="text-muted" href="https://twitter.com/corbin_page" target="_blank">Contact</a>
      </div>
    </div>
  </nav>
  </body>
</html>
